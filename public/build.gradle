apply plugin: 'org.springframework.boot'
apply plugin: 'war'

war {
    baseName = 'smartblog-public'
    archiveName = "${baseName}.${extension}"

}

dependencies {
    compile project(':common')

}
